#pragma once

enum cutType {
	NONE = 0,
	BOX = 1,
	BALL = 2,
	PLANE = -1 //not sure if necessary
	, TUBE = 3
	, WINDOW = 4
};

enum TexType {
	_NONE = 0,
	CHECKERED = 1,
	TEXTURE,
	NORMALS
};

